# <Title: what this guide gets done>

> Trace: <username>/<YYYY-MM-DD_topic-slug> · Author: <username> · Date: <YYYY-MM-DD>
> Before deviating from this guide, read LOG.md — it lists approaches that already failed.

## What this does

One short paragraph: the task, the inputs, the outputs.

## Prerequisites

- Environment: modules / conda envs needed, with exact create/load commands
- Data assumptions: input formats, where reference files live
- Cluster: partition, memory, CPU, and runtime that actually worked

## Steps

Numbered, copy-pasteable steps. Reference files in `scripts/` rather than
inlining long code. Note anything that must be edited per-run (sample sheets,
paths).

## Verifying it worked

What outputs to check and what "good" looks like (QC numbers, file sizes,
sanity plots).

## Known limits

Where this recipe stops applying (sample counts, organisms, data types).
