# Session log: <title>

> Trace: <username>/<YYYY-MM-DD_topic-slug> · Author: <username> · Date: <YYYY-MM-DD>

## Goal

What the session set out to do, and the context (project, data, deadline).

## Dead ends — REQUIRED, fill even if empty

The most valuable section. One block per approach that was tried and
abandoned. "None" is only acceptable if the first approach worked outright.

### Dead end: <short name of approach>
- **What was tried:** exact commands / tools / versions
- **What happened:** the actual error or symptom, verbatim where possible
- **Why it failed:** root cause if known, best guess otherwise
- **Verdict:** `do-not-retry` | `retry-if: <condition changes, e.g. module fixed, more memory>`

## What worked

Short narrative of the path that succeeded and why it was chosen over the
alternatives. Details live in GUIDE.md — don't duplicate them here.

## Environment snapshot

Modules + versions, conda env name and key packages, partition/resources,
relevant paths as of the session date.

## Open questions

Anything unresolved or worth improving next time.
